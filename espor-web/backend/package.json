{
  "name": "espor-web-backend",
  "version": "1.0.0",
  "description": "Backend for espor web platform",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "migrate": "node -e \"const pool = require('./src/config/database'); const fs = require('fs'); const sql = fs.readFileSync('./src/config/database.sql', 'utf8'); pool.query(sql).then(() => { console.log('Migration completed'); process.exit(0); }).catch(err => { console.error('Migration error:', err); process.exit(1); });\""
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.2",
    "pg": "^8.11.3",
    "socket.io": "^4.6.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
